# 260802 손님 계정 보관함 버튼 명칭 변경

## 1. 개요
손님 계정의 `조향기록서 보관함(GuestMyPage)` 내 목록 버튼 명칭을 기존 `기록서 보기`에서 **`접수상태 보기`**로 변경하였습니다.

## 2. 세부 변경 사항
- `src/components/Customer/GuestMyPage.tsx`:
  - 목록의 액션 버튼 문구를 `기록서 보기` ➔ **`접수상태 보기`**로 변경.
  - 버튼 아이콘을 `FileText` 아이콘으로 변경하여 접수 상태 확인 목적에 부합하도록 시각적 UX 최적화.
